# PHPNukeReal
ping

program dca;
var i, j, k: integer;
begin
    for i = 1..m
        for j = 1..n
            for k = 1 max (m, n)
                if (a[i, k] + a[k, j] > a[i, j])
                    a[i, j] = a[i, k] + a[k, j]

Dijktra 
